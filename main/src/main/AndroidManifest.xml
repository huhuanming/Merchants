<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.merchants.main" >
    <!-- 高德地图信息 -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.READ_PHONE_STATE"/>
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <uses-permission android:name="android.permission.CHANGE_CONFIGURATION"/>
    <!-- 高德地图信息 -->

    <!--数据库名字及版本号-->
    <meta-data
        android:name="AA_DB_NAME"
        android:value="Data.db" />
    <meta-data
        android:name="AA_DB_VERSION"
        android:value="2" />
    <!--数据库名字及版本号-->

    <application
        android:name=".Application.MerchantsApplication"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:theme="@android:style/Theme.Light.NoTitleBar"
            android:name=".Activity.MerchantsLogin"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:theme="@style/MyTheme"
            android:name=".Activity.MerchantsMenu"
            android:icon="@drawable/lingjiaxiaochu"
            android:label="@string/store_management" >
        </activity>

        <activity
            android:name=".Activity.MenuManagementNewFood"
            android:icon="@drawable/lingjiaxiaochu"
            android:theme="@style/MyTheme"
            android:label="@string/new_food" >
        </activity>

        <activity
            android:name=".Activity.OrderManagementDetails"
            android:icon="@drawable/lingjiaxiaochu"
            android:theme="@style/MyTheme"
            android:label="@string/order_details" >
        </activity>

        <activity
            android:name=".Activity.StoreManagementText"
            android:icon="@drawable/lingjiaxiaochu"
            android:theme="@style/MyTheme"
            android:label="@string/shop_notices" >
        </activity>

        <activity
            android:name=".Activity.StoreManagementNumber"
            android:icon="@drawable/lingjiaxiaochu"
            android:theme="@style/MyTheme"
            android:label="@string/app_name" >
        </activity>
    </application>

</manifest>
